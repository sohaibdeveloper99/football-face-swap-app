[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["echo 'No build step required for Node.js API'"]

[start]
cmd = "cd backend && npm start"
